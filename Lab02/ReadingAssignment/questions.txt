I can not create a constructor method to built a DVD by title then create another constructor method to built a DVD by category. This is because two constructor methods both have the same parameter type (String) (due to Method Overleading). 
