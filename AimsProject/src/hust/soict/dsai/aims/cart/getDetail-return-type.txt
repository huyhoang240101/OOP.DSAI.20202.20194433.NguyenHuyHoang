The return type of the getDetail() method should be an ArrayList. Because ArrayList is a variable length Collection class 
so the DVDs with different number of attributes will be easier to work with. Besides, ArrayList is also very easy to access 
information.